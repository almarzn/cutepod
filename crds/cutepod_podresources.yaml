---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: podresources.cutepod
spec:
  group: cutepod
  names:
    kind: PodResource
    listKind: PodResourceList
    plural: podresources
    shortNames:
    - cp
    singular: podresource
  scope: Namespaced
  versions:
  - name: resource
    schema:
      openAPIV3Schema:
        description: PodResource represents a pod resource that implements the Resource
          interface
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CutePodSpec defines the specification for a pod
            properties:
              containers:
                items:
                  type: string
                minItems: 1
                type: array
              restartPolicy:
                default: Always
                enum:
                - Always
                - OnFailure
                - Never
                type: string
            required:
            - containers
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
