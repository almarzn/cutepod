---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: volumeresources.cutepod
spec:
  group: cutepod
  names:
    kind: VolumeResource
    listKind: VolumeResourceList
    plural: volumeresources
    shortNames:
    - cv
    singular: volumeresource
  scope: Namespaced
  versions:
  - name: resource
    schema:
      openAPIV3Schema:
        description: VolumeResource represents a volume resource that implements the
          Resource interface
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CuteVolumeSpec defines the specification for a volume with
              Kubernetes-style volume types
            properties:
              emptyDir:
                description: EmptyDirVolumeSource represents a temporary directory
                  that shares a pod's lifetime
                properties:
                  medium:
                    description: StorageMedium defines ways that storage can be allocated
                      to a volume
                    type: string
                  sizeLimit:
                    type: string
                type: object
              hostPath:
                description: HostPathVolumeSource represents a host path mapped into
                  a pod
                properties:
                  path:
                    minLength: 1
                    type: string
                  type:
                    description: HostPathType represents the type of host path
                    enum:
                    - DirectoryOrCreate
                    - Directory
                    - FileOrCreate
                    - File
                    - Socket
                    - CharDevice
                    - BlockDevice
                    type: string
                required:
                - path
                type: object
              securityContext:
                description: VolumeSecurityContext holds security configuration for
                  volumes
                properties:
                  owner:
                    description: VolumeOwnership defines ownership settings for volumes
                    properties:
                      group:
                        format: int64
                        type: integer
                      user:
                        format: int64
                        type: integer
                    type: object
                  seLinuxOptions:
                    description: SELinuxVolumeOptions defines SELinux options for
                      volume mounts
                    properties:
                      level:
                        type: string
                    type: object
                type: object
              type:
                description: VolumeType represents the type of volume
                enum:
                - hostPath
                - emptyDir
                - volume
                type: string
              volume:
                description: VolumeVolumeSource represents a named Podman volume
                properties:
                  driver:
                    type: string
                  options:
                    additionalProperties:
                      type: string
                    type: object
                type: object
            required:
            - type
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
