---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: networkresources.cutepod
spec:
  group: cutepod
  names:
    kind: NetworkResource
    listKind: NetworkResourceList
    plural: networkresources
    shortNames:
    - cn
    singular: networkresource
  scope: Namespaced
  versions:
  - name: resource
    schema:
      openAPIV3Schema:
        description: NetworkResource represents a network resource that implements
          the Resource interface
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CuteNetworkSpec defines the specification for a network
            properties:
              driver:
                default: bridge
                type: string
              gateway:
                pattern: ^([0-9]{1,3}\.){3}[0-9]{1,3}$
                type: string
              options:
                additionalProperties:
                  type: string
                type: object
              subnet:
                pattern: ^([0-9]{1,3}\.){3}[0-9]{1,3}/[0-9]{1,2}$
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
